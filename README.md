# Real-Time Face Attendance System

A modular Python application using OpenCV YuNet ONNX for face detection.

## Structure
- `main.py`: Runs face detection stream.
- `src/`: Modular Python components (`camera.py`, `detector.py`, etc.).
- `models/`: ONNX weights directory.

## Setup
